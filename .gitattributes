# Treat bun.lock as text during merge to prevent conflicts in the lockfile.
# After pulling changes, run 'pnpm install' to regenerate the lockfile correctly.
pnpm-lock.yaml                    merge=text
